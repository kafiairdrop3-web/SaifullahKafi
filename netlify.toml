[build]
  command = "npm run build"
  publish = "dist"

# Handle SPA routing so reloading on /subject/physics or /admin works cleanly
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
